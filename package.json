{
  "name": "node-ts-di",
  "version": "1.0.0-alpha.1",
  "main": "dist/index",
  "typings": "dist/index",
  "repository": "https://github.com/KostyaTretyak/node-ts-di",
  "author": "Третяк Костя",
  "license": "MIT",
  "devDependencies": {
    "@types/jasmine": "^2.8.8",
    "@types/node": "^10.9.4",
    "jasmine": "^3.2.0",
    "reflect-metadata": "^0.1.12",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  },
  "scripts": {
    "prebuild": "npm run lint",
    "compile": "tsc",
    "build:test": "rimraf dist-test && tsc --project test",
    "test": "npm run build:test && jasmine",
    "lint": "tslint -p tsconfig.json"
  }
}
